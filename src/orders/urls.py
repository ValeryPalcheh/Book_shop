
from django.urls import path, include
from . import views

app_name = "orders"

urlpatterns = [  
    # path("cart/", views.view_cart, name="view-cart"),
    # path("add-book/", views.add_book_to_cart_view, name="add_book_to_cart"),
    # path("evaluate/", views.evaluate_cart, name="evaluate-cart"),
    ]